import system'routines.
import extensions.

program =
[
    console writeLine(console readLine;
                              split;
                              selectEach(%"convertorOp.toInt[0]");
                              summarize).
].
